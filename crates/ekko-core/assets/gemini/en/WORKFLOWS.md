# Ekko Workflows (Gemini CLI)

Gemini CLI uses `GEMINI.md` as hierarchical instructional context (global + project + subdir).

Ekko does not inject custom slash-commands for Gemini CLI; instead, it ships workflow templates for Codex/Claude and a managed `GEMINI.md` block for Gemini.
