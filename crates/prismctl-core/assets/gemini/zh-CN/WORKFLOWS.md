# Prismctl Workflows（Gemini CLI）

Gemini CLI 支持通过 `GEMINI.md` 提供系统提示/上下文，但目前 Prismctl 不尝试为 Gemini CLI 注入类似 Claude/Codex 的“自定义命令”工作流文件。

建议：
- 在项目根目录放置 `.gemini/GEMINI.md` 作为项目级上下文
- 需要结构化工作流时，可参考 Prismctl 的六阶段工作流（研究→构思→计划→执行→优化→评审）

