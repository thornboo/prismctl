# Examples

These files are **examples only**. Do not commit real API keys.

- `codex-config.toml`: example `~/.codex/config.toml` snippet
- `codex-auth.json`: example `~/.codex/auth.json` snippet (Ekko uses `EKKO_CODEX_API_KEY`)
- `claude-settings.json`: example `~/.claude/settings.json` snippet
- `gemini.env`: example Ekko-managed block for `~/.gemini/.env`

